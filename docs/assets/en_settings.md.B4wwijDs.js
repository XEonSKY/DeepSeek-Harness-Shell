import{_ as t,o,c as n,a2 as r}from"./chunks/framework.BXzK3EA4.js";const p=JSON.parse('{"title":"Settings","description":"","frontmatter":{},"headers":[],"relativePath":"en/settings.md","filePath":"en/settings.md","lastUpdated":null}'),s={name:"en/settings.md"};function a(i,e,l,d,h,c){return o(),n("div",null,[...e[0]||(e[0]=[r('<h1 id="settings" tabindex="-1">Settings <a class="header-anchor" href="#settings" aria-label="Permalink to &quot;Settings&quot;">​</a></h1><p>The <strong>Settings</strong> page groups options on the left: General, Appearance, Network, Environment, Kernel, Terminal, Shortcuts, Webview, About. Setting cards can be collapsed from their headers.</p><h2 id="general" tabindex="-1">General <a class="header-anchor" href="#general" aria-label="Permalink to &quot;General&quot;">​</a></h2><ul><li><strong>Working directory</strong>: where dsh starts; empty uses <code>workspace</code> under the config directory.</li><li><strong>Port</strong>: Auto (recommended; picks a free port from 3080) or manual.</li><li><strong>Close behavior</strong>: hide to tray or quit; “ask each time” or “remember choice”.</li></ul><h3 id="new-tab-search" tabindex="-1">New tab &amp; search <a class="header-anchor" href="#new-tab-search" aria-label="Permalink to &quot;New tab &amp; search&quot;">​</a></h3><ul><li><p><strong>Default search engine</strong>: used when the address bar receives something that is not a URL, and by the built-in new tab page (Baidu / Sogou / 360 / Bing / Google / DuckDuckGo).</p></li><li><p><strong>New tab</strong>: what the <strong>+</strong> button in the tab bar opens — the <strong>built-in new tab page</strong> (a search box plus shortcuts) or a <strong>custom URL</strong>.</p></li><li><p><strong>Site shortcuts</strong>: a list of name + URL entries shown on the built-in new tab page for one-click access.</p></li><li><p><strong>Restore defaults</strong>: reset everything and restart dsh.</p></li></ul><h2 id="appearance" tabindex="-1">Appearance <a class="header-anchor" href="#appearance" aria-label="Permalink to &quot;Appearance&quot;">​</a></h2><ul><li><strong>Theme</strong>: System / Light / Dark — takes effect immediately.</li><li><strong>Color scheme</strong>: seven presets (default blue / geek purple / forest green / cyan / warm orange / rose red / graphite). A scheme sets both the <strong>accent color</strong> and the <strong>page / sidebar backgrounds</strong>, with light and dark variants handled automatically (switching Theme recomputes it). Light-vs-dark itself stays with Theme above, and “default blue” keeps the original look.</li><li><strong>Language (two-level menu)</strong>: pick 中文 / English, plus an extended translation (zh: Simplified / Anime / Classical Chinese / Traditional Chinese; en: English / Pirate / Shakespearean). Extended translations are <strong>text overlays</strong>, apply only to the current language, and never modify dsh settings.</li><li><strong>Zoom</strong>: 50%–200% (applies to the shell window and the embedded webview).</li><li><strong>Ignore OS scaling</strong>: prompts for a restart to take effect.</li></ul><h2 id="network" tabindex="-1">Network <a class="header-anchor" href="#network" aria-label="Permalink to &quot;Network&quot;">​</a></h2><ul><li><strong>npm registry</strong>: official registry / npmmirror.</li><li><strong>Proxy</strong>: enable + protocol (HTTP / SOCKS5) + host + port + <strong>proxy scope</strong> (npm installs/downloads, Node download &amp; deploy, kernel update checks). Currently the npm scope actually routes through the proxy; Node download and update-check proxy support are follow-ups.</li></ul><h2 id="environment" tabindex="-1">Environment <a class="header-anchor" href="#environment" aria-label="Permalink to &quot;Environment&quot;">​</a></h2><p>Where the Node that runs the kernel comes from. <strong>The three tabs are the three choices</strong> (clicking a tab switches it; restart dsh afterwards):</p><ul><li><strong>Bundled</strong>: shows the Node version bundled with Electron plus the Chromium version. Both upgrade with the app and cannot be updated separately.</li><li><strong>System</strong>: shows the system Node’s current version and the latest LTS; when it is behind, a <strong>Download Node</strong> button appears. A system-wide Node is yours to upgrade (installer / winget / nvm / brew …) — the app never touches system installs; switch to Local deploy to let the app manage it.</li><li><strong>Local deploy</strong>: the same current/latest comparison; when it is missing or behind it offers <strong>Deploy Node</strong> / <strong>Update to vX</strong>. You can also <strong>pick any version to download / switch to</strong> (the list comes from nodejs.org, with an “Include non-LTS (Current)” checkbox); switching <strong>replaces</strong> the copy in the config directory — there are no side-by-side versions. If dsh is running on that local Node, the app asks to stop it first and restarts it afterwards.</li></ul><blockquote><p>“Latest LTS” comes from nodejs.org’s release index and is cached in the main process for 10 minutes; offline or through a broken proxy it shows “Could not fetch the latest version”. The kernel needs <strong>Node ≥ 20</strong>: picking a lower major version in the list warns you — it cannot run the kernel.</p></blockquote><p>After changing the runtime, click <strong>Apply now</strong> at the bottom to restart dsh and switch immediately. If the selected runtime is unavailable (no system Node ≥ 20, or nothing deployed locally) the button is <strong>disabled</strong> and says why — applying means restarting dsh, which would simply fail to start.</p><h3 id="npm-source" tabindex="-1">npm source <a class="header-anchor" href="#npm-source" aria-label="Permalink to &quot;npm source&quot;">​</a></h3><p>The npm used when installing the kernel. <strong>Again: the three tabs are the three choices.</strong></p><ul><li><strong>Bundled</strong>: downloaded from the registry into the config directory on first use; managed by the app.</li><li><strong>System</strong>: uses the npm installed on your system; updating performs a global install — <strong>this really changes your global npm</strong>.</li><li><strong>Local Node</strong>: uses the npm that ships with the locally deployed Node; updating installs into that Node under the config directory.</li></ul><p>Each tab shows the <strong>current version</strong> and the <strong>latest on the registry</strong>; when it is behind or not ready yet, a <strong>Download npm / Update npm</strong> button appears. Below the tabs there is one <strong>version picker</strong> shared by all three (with an “Include pre-releases” checkbox) that can install any historical version — installing targets the source of <strong>the tab selected above</strong>.</p><blockquote><p>The npm source is only used when installing the kernel with kernel source = Bundled; changing it does <strong>not</strong> need a dsh restart, it applies to the next kernel install / update. If <strong>System</strong> fails with a permission error (EPERM), the global npm prefix usually points at the system Node directory: run as administrator, or switch to one of the other two sources (they install into the config directory).</p></blockquote><h2 id="kernel" tabindex="-1">Kernel <a class="header-anchor" href="#kernel" aria-label="Permalink to &quot;Kernel&quot;">​</a></h2><ul><li><strong>Start / Stop / Restart</strong>: control the dsh server directly, with a live status tag.</li><li><strong>Apply now</strong>: persist runtime changes and restart dsh.</li><li><strong>Kernel source</strong>: Bundled (installed into the app dir, run with the built-in Node) or Global.</li><li><strong>Launcher path</strong>: shown only in Global mode; pick the file with a file dialog.</li><li><strong>Kernel updates</strong>: check on startup, include pre-releases, and a manual check / update. <strong>No notification is shown when you are already up to date</strong> — the result appears as a line under the Check button (including the version the remote resolved to).</li><li><strong>Version / uninstall</strong>: browse and <strong>install / switch</strong> to any historical version; <strong>Uninstall</strong> stops dsh first, then shows the install wizard.</li></ul><div class="warning custom-block"><p class="custom-block-title">Upgrade / switch / uninstall</p><p>These operations <strong>force-stop all running dsh instances first</strong> (they ask for confirmation if one is running). After an upgrade or switch, dsh restarts automatically; on failure the app detects the occupation cause and reports it.</p></div><h2 id="terminal" tabindex="-1">Terminal <a class="header-anchor" href="#terminal" aria-label="Permalink to &quot;Terminal&quot;">​</a></h2><ul><li>Live dsh output (stdout / stderr) with ANSI colors; toggle <strong>auto-scroll</strong> or <strong>clear</strong> the display.</li><li>The terminal is <strong>read-only</strong> (no keyboard input); a placeholder is shown while dsh has produced no output.</li><li>Leaving and coming back re-reads the recent output (server-side ring buffer, capped at 5000 lines).</li><li>The title bar no longer carries a terminal button: press <code>Ctrl</code> / <code>Cmd</code> + <code>T</code> to toggle between DeepSeek UI and the terminal.</li></ul><h2 id="shortcuts" tabindex="-1">Shortcuts <a class="header-anchor" href="#shortcuts" aria-label="Permalink to &quot;Shortcuts&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Shortcut</th><th>Default</th><th>Scope</th></tr></thead><tbody><tr><td>Return to the main window</td><td><code>Ctrl</code> / <code>Cmd</code> + <code>Alt</code> + <code>H</code></td><td><strong>System-wide</strong> — brings the app’s main window to the front from any program (also when minimized or hidden to the tray)</td></tr><tr><td>Toggle the terminal view</td><td><code>Ctrl</code> / <code>Cmd</code> + <code>T</code></td><td>In-app: switches between DeepSeek UI and Settings → Terminal</td></tr><tr><td>DevTools console</td><td><code>F12</code></td><td>In-app: only active when Developer mode is on (About page)</td></tr></tbody></table><ul><li>Click <strong>Change</strong>, then press a combination (<code>Esc</code> cancels); <strong>Clear</strong> disables it and <strong>Reset</strong> restores the default above.</li><li>Letters, digits and F1–F24 only, and at least one modifier is required — otherwise typing would be swallowed.</li><li>The system-wide one may be taken by another program; the page tells you when that happens — just pick another combination.</li><li>Changes apply immediately; no restart needed.</li></ul><h2 id="webview" tabindex="-1">Webview <a class="header-anchor" href="#webview" aria-label="Permalink to &quot;Webview&quot;">​</a></h2><p>How the embedded pages (kernel UI, web chat and every dynamic tab) render, and what they identify as.</p><ul><li><p><strong>Hardware acceleration</strong>: on by default. Turning it off lowers resource usage and helps with old drivers, at the cost of choppier scrolling and animation. It can only be decided while the app starts, so changing it needs an <strong>app restart</strong> (the page offers to restart right away).</p></li><li><p><strong>UserAgent</strong>: leave it empty to use the default. The default is generated live from the current platform and versions, like:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/130.0.6723.58 Safari/537.36 XEonSKY/0.1.5-alpha-4</span></span></code></pre></div><p>The platform token follows each platform’s real Chrome string; after <code>Chrome/</code> is the current Chromium version, and after <code>XEonSKY/</code> the app version.</p></li><li><p>A custom UA applies to <strong>new requests</strong> immediately; already-loaded pages pick it up after a refresh (or a restart).</p></li></ul><h2 id="about" tabindex="-1">About <a class="header-anchor" href="#about" aria-label="Permalink to &quot;About&quot;">​</a></h2><ul><li>Shows the app version and runtime architecture.</li><li><strong>Auto update</strong>: check for a new app version on startup and download it in the background; <strong>Include pre-releases</strong>; <strong>Developer mode</strong> (F12 toggles DevTools by default — change it on the Shortcuts page).</li></ul><blockquote><p>Theme and the base language still mirror dsh’s own <code>~/.dsh/settings.yaml</code>; the app’s own settings (General / Network / Kernel / About, etc.) live in the app <strong>config directory</strong> <code>settings.json</code> (default <code>~/.config/dsh_shell</code>, <code>dsh_shell_dev</code> in dev; selectable in the wizard/settings, with migration).</p></blockquote>',34)])])}const u=t(s,[["render",a]]);export{p as __pageData,u as default};
