/**
 * English "pirate" overlay translation (text overlay).
 * Only keys that change are listed; merged over the en base at runtime. Extend freely.
 */
export default {
  app: {
    nav: { settings: 'Me Orders' },
    reload: 'Re-swab the decks'
  },
  sv: {
    cap: "Ship's Orders",
    general: { browse: 'Look about…' }
  }
} as Record<string, unknown>
