/**
 * English "shakespearean" overlay translation (text overlay).
 * Only keys that change are listed; merged over the en base at runtime. Extend freely.
 */
export default {
    app: {
        nav: { settings: 'The Settings' },
        reload: 'Refresh the page anon'
    },
    sv: {
        cap: 'The Settings'
    }
} as Record<string, unknown>
